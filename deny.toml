# cargo-deny policy. Single source of truth, consumed by:
#   - .github/workflows/security.yml      (cargo deny check on every PR/push)
#   - .github/workflows/supply-chain.yml  (weekly + manual re-audit)
#
# Advisory *detection* is also covered passively by GitHub Dependabot
# alerts; this file is what enforces the license / bans / sources policy
# and gives a hard, reproducible gate.

[advisories]
yanked = "deny"
# `unmaintained`/`vulnerability` severities are evaluated by default; a
# new advisory against the committed lockfile fails the run.

[licenses]
allow = [
  "MIT",
  "Apache-2.0",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "ISC",
  "Unicode-3.0",
  "Unicode-DFS-2016",
  "MPL-2.0",
  "Zlib",
  # webpki-roots (Mozilla CA bundle, pulled via the TLS stack) ships its
  # baked-in root-cert data under CDLA-Permissive-2.0 — a permissive,
  # no-source-disclosure data license. Scopes only to that crate's data.
  "CDLA-Permissive-2.0",
]

[bans]
multiple-versions = "warn"
wildcards = "deny"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
